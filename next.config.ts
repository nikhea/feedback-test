import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "kzmpi0ptowvyz1snxvtw.lite.vusercontent.net",
    ],
  },
  /* config options here */
};

export default nextConfig;
