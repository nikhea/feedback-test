import React from "react";

const Page = () => {
  return <div>page</div>;
};

export default Page;

{
  /* <h1>Name: {username}</h1>
      <input
        type="text"
        className="bg-red-100 border"
        value={username}
        onChange={(e) => setUserName(e.target.value)}
      />
      <h1>email: {email}</h1>
      <input
        type="text"
        className="bg-red-100 border"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
      />
      <div>
        <h1>Count: {count}</h1>
        <button onClick={() => increasecount(5)}>increment</button>
        <button onClick={() => decreasecount(5)}>decrement</button>
        <button onClick={() => reset()}>reset</button>
      </div> */
}
// const { username, email, setEmail, setUserName } = useUserStore();
// const { count, increasecount, decreasecount, reset } = useCounterStore();

// import { useCounterStore } from "@/store/counterStore";
// import { useUserStore } from "@/store/userStore";
